# Support for unicode, helps render unicode code points for fancy status lines
alias tmux='tmux -u'

# Useful aliases for tmux
alias tn='tmux new-session'
alias ta='tmux attach-session -t'
alias tl='tmux list-session'
